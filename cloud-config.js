/* Preencha depois de criar o projeto gratuito no Supabase.
   Project URL e anon public key (Settings → API).
   A chave anon é pensada para ficar no aplicativo; os dados
   ficam protegidos pelo código do escritório. */
window.AGENDA_CLOUD = {
  supabaseUrl: '',
  supabaseAnonKey: '',
};
